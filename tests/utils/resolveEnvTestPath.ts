/** Re-export so tests keep a stable import; implementation lives under `backend/` (no `../tests` from shared/config). */
export { resolveEnvTestPath } from "../../backend/shared/config/resolveEnvTestPath.ts";
