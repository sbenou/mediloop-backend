
import { useAuth } from "@/hooks/auth/useAuth";
import { useState, useEffect } from "react";

export const AuthDebugger = () => {
  // Always return null to completely remove the debugger
  return null;
};

export default AuthDebugger;
