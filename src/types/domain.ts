/**
 * Shared domain types for the Mediloop app (Neon + Deno API).
 * Prefer importing from here or specific modules (`clinical`, `availability`, …)
 * instead of legacy `@/types/supabase`.
 */

export * from "./notifications";
export * from "./availability";
export * from "./profiles";
